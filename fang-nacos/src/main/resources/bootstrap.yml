#服务名称
spring:
  application:
    name: fang-nacos
  cloud:
    nacos:
      discovery:
        # 配置Nacos地址
        server-addr: http://localhost:8848
        ##  作为配置中心的配置
      config:
        # Nacos地址
        server-addr: http://localhost:8848
        # 这里我们获取的yaml格式的配置
        file-extension: yaml

management:
  endpoint:
    web:
      exposure:
        include: '*'



